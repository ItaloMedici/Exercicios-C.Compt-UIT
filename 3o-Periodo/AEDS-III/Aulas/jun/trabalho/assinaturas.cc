#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct aluno {
  char nome[20];
  char assinatura[20];
} Aluno;

int main () {
  char linha[100];
  scanf("%[^\n]s", linha);
  int alunosTurma = ;
  
  printf("%d", atoi(linha));
  
  Aluno alunos[alunosTurma];



/* 
  char nomeAlunoAtual[20] = "";
  char assiaturaAlunoAtual[20] = "";

  int erros = 0;

  if(strcmp(alunos->nome, nomeAlunoAtual)) {
    if(!strcmp(alunos->assinatura, assiaturaAlunoAtual))
      erros++;
  } else {
    // avançar nomes;
  }
 */
  return 0;
}